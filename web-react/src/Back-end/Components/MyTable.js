import { Box, Button, Typography } from "@mui/material"
import MyOption from "./MyOption"

const MyTable = (props) => {

    return(
        <Box>
            <Typography variant="h4">My Table</Typography>
            <MyOption/>
        </Box>
    )
}
export default MyTable
